The system displays /public/logo.svg by default (a placeholder that approximates
the brand mark).

To swap in the real logo, drop your high-resolution PNG file at:

    public/logo.png

The app and PDF letterhead will pick it up automatically — no code changes needed.
Recommended size: 512x512 (square, transparent background works best for print).
